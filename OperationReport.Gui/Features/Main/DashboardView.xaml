<UserControl x:Class="AetherSystem.OperationReport.Gui.Features.Main.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:AetherSystem.OperationReport.Gui.Features.Main"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:DashboardViewModel}">
  <UserControl.Resources>
    <ResourceDictionary>
      <DataTemplate DataType="{x:Type local:SampleDataViewModel}">
        <local:SampleDataView />
      </DataTemplate>
      <DataTemplate x:Key="MenuItemTemplate" DataType="{x:Type local:DashboardContent}">
        <Grid Height="48">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding RelativeSource={RelativeSource AncestorType={x:Type mah:HamburgerMenu}}, Path=CompactPaneLength}" />
            <ColumnDefinition />
          </Grid.ColumnDefinitions>
          <ContentControl Grid.Column="0"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Content="{Binding Icon}"
                          Focusable="False"
                          IsTabStop="False" />
          <TextBlock Grid.Column="1"
                     VerticalAlignment="Center"
                     FontSize="16"
                     Text="{Binding Title}" />
        </Grid>
      </DataTemplate>
    </ResourceDictionary>
  </UserControl.Resources>
  <mah:HamburgerMenu x:Name="HamburgerMenuControl"
                     HamburgerVisibility="Collapsed"
                     HamburgerWidth="48"
                     IsPaneOpen="True"
                     ItemCommand="{Binding ChangePageCommand}"
                     ItemTemplate="{StaticResource MenuItemTemplate}"
                     ItemsSource="{Binding Pages}"
                     SelectedItem="{Binding CurrentPage}"
                     ItemInvoked="HamburgerMenuControl_OnItemInvoked">
    <mah:HamburgerMenu.ContentTemplate>
      <DataTemplate DataType="{x:Type local:DashboardContent}">
        <ContentControl Content="{Binding}" />
      </DataTemplate>
    </mah:HamburgerMenu.ContentTemplate>
  </mah:HamburgerMenu>
</UserControl>
